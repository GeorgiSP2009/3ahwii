datasource db {
    provider = "sqlite"
    url = env("DATABASE_URL")
}

generator client {
    provider = "prisma_client"
    output = "client"
    runtime = "deno"
}

model hello {
    id String @id @default(cuid())
    name String
}